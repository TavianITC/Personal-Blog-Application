@{
    ViewData["Title"] = "Access Denied";
    Layout = "_Layout";
}

<div style="text-align:center; padding: 4rem 0;">
    <h1 style="font-family:var(--font-display);font-size:3rem;letter-spacing:-0.03em">
        403
    </h1>
    <p style="color:var(--gray-600);margin:1rem 0 2rem">
        You don't have permission to access this page.
    </p>
    <a asp-controller="Blogs" asp-action="Index"
       style="background:var(--black);color:#fff;padding:0.6rem 1.5rem;s
        border-radius:2px;text-decoration:none;font-size:0.875rem">
        Go to my blogs
    </a>
</div>